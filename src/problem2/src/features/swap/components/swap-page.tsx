import { SwapForm } from './swap-form';
import { Container, Center } from '@mantine/core';

export function SwapPage() {
  return (
    <Container pt="xl">
      <Center>
        <SwapForm />
      </Center>
    </Container>
  );
}
