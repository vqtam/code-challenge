# Currency Swap Form

Single-page currency swap form built with Vite, React 19, TypeScript, Mantine 9, React Hook Form, and Zod.

## Scripts

```bash
pnpm install
pnpm dev
pnpm lint
pnpm typecheck
pnpm build
```

## Environment

Copy `.env.example` to `.env` before running the app.
